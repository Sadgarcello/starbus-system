# Copy to .env.railway (gitignored). Used ONLY by: npm run set-password
# Fill from Railway → your MySQL service → Connect (public / TCP URL).
# Keeps .env on 127.0.0.1 for local app while scripts hit cloud DB.

DB_HOST=
DB_PORT=
DB_USER=root
DB_PASSWORD=
DB_NAME=railway
DB_SSL=1
